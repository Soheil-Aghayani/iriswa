# Iranian Solid Waste Management Association Website

This repository contains the source code for the Iranian Solid Waste Management Association website.

## Building the Site

To build the site and generate the HTML files, run the following command:

```bash
python build.py
```

This script parses the markdown content and layout templates to generate the static site pages in the root directory.